# 🎨 CipherWall VPN - Architecture & Flow Diagrams

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        CipherWall VPN System                     │
└─────────────────────────────────────────────────────────────────┘

CLIENT SIDE (Your Laptop)          SERVER SIDE (Italy)
┌─────────────────────┐            ┌─────────────────────┐
│   Applications      │            │   Internet          │
│   (Browser, etc)    │            │   (Websites)        │
└──────────┬──────────┘            └──────────▲──────────┘
           │                                   │
           ▼                                   │
┌─────────────────────┐            ┌──────────┴──────────┐
│  TUN Interface      │            │  NAT / iptables     │
│  (10.8.0.2/24)      │            │  (MASQUERADE)       │
└──────────┬──────────┘            └──────────▲──────────┘
           │                                   │
           ▼                                   │
┌─────────────────────┐            ┌──────────┴──────────┐
│  CipherWall Client  │            │  TUN Interface      │
│  - Encrypt          │            │  (10.8.0.1/24)      │
│  - Add HMAC         │            └──────────▲──────────┘
│  - Route packets    │                       │
└──────────┬──────────┘            ┌──────────┴──────────┐
           │                       │  CipherWall Server  │
           │                       │  - Decrypt          │
           ▼                       │  - Verify HMAC      │
┌─────────────────────┐            │  - Route packets    │
│   UDP Socket        │            └──────────▲──────────┘
│   (Local)           │                       │
└──────────┬──────────┘            ┌──────────┴──────────┐
           │                       │   UDP Socket        │
           │   Encrypted Tunnel    │   (Port 1194)       │
           └───────────────────────►                     │
                Internet            └─────────────────────┘
```

---

## 🔄 Packet Flow - Outgoing (Client → Internet)

```
Step 1: Application sends data
┌─────────────┐
│   Browser   │  "GET https://google.com"
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  OS Network │  Routes to TUN (10.8.0.0/24)
│    Stack    │
└──────┬──────┘
       │
       ▼

Step 2: Client encrypts
┌─────────────┐
│     TUN     │  Read IP packet
│  (10.8.0.2) │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   Client    │  1. Encrypt with AES-256-CFB
│   Process   │  2. Add HMAC-SHA256
└──────┬──────┘  3. Send via UDP
       │
       ▼
       │  [HMAC][IV][Ciphertext]
       │
       ▼

Step 3: Network transmission
┌─────────────┐
│     UDP     │  Encrypted packet sent
│   Socket    │  to server:1194
└──────┬──────┘
       │
       │  Internet (Encrypted)
       ▼
┌─────────────┐
│   Server    │  Receives encrypted packet
│ UDP Socket  │
│  :1194      │
└──────┬──────┘
       │
       ▼

Step 4: Server decrypts
┌─────────────┐
│   Server    │  1. Verify HMAC
│   Process   │  2. Decrypt with AES
└──────┬──────┘  3. Extract IP packet
       │
       ▼
┌─────────────┐
│     TUN     │  Write decrypted IP packet
│  (10.8.0.1) │
└──────┬──────┘
       │
       ▼

Step 5: NAT and forward
┌─────────────┐
│  iptables   │  MASQUERADE (NAT)
│     NAT     │  Source: 10.8.0.2 → Server Public IP
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  Internet   │  Request reaches Google
│  (Google)   │  Sees: Italy server IP
└─────────────┘
```

---

## 🔙 Packet Flow - Incoming (Internet → Client)

```
Step 1: Response arrives
┌─────────────┐
│  Internet   │  Response from Google
│  (Google)   │  Destination: Server Public IP
└──────┬──────┘
       │
       ▼

Step 2: NAT translation
┌─────────────┐
│  iptables   │  Reverse NAT
│     NAT     │  Destination: Server IP → 10.8.0.2
└──────┬──────┘
       │
       ▼

Step 3: Route to client
┌─────────────┐
│     TUN     │  Routing table:
│  (10.8.0.1) │  10.8.0.2 → TUN interface
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   Server    │  1. Encrypt with AES-256-CFB
│   Process   │  2. Add HMAC-SHA256
└──────┬──────┘  3. Send via UDP to client
       │
       ▼
       │  [HMAC][IV][Ciphertext]
       │
       ▼

Step 4: Network transmission
┌─────────────┐
│   Server    │  Send encrypted packet
│ UDP Socket  │  to client IP:port
└──────┬──────┘
       │
       │  Internet (Encrypted)
       ▼
┌─────────────┐
│   Client    │  Receives encrypted packet
│ UDP Socket  │
└──────┬──────┘
       │
       ▼

Step 5: Client decrypts
┌─────────────┐
│   Client    │  1. Verify HMAC
│   Process   │  2. Decrypt with AES
└──────┬──────┘  3. Extract IP packet
       │
       ▼
┌─────────────┐
│     TUN     │  Write decrypted IP packet
│  (10.8.0.2) │
└──────┬──────┘
       │
       ▼

Step 6: Deliver to application
┌─────────────┐
│  OS Network │  Routes to application
│    Stack    │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   Browser   │  Receives response
└─────────────┘  Displays webpage
```

---

## 🔐 Encryption Flow

```
PLAINTEXT PACKET
┌────────────────────────────────┐
│  IP Header | TCP/UDP | Data    │  Original packet
└────────────────────────────────┘
              │
              │  Step 1: Generate IV
              ▼
┌────────────────────────────────┐
│  Random IV (16 bytes)          │  Initialization Vector
└────────────────────────────────┘
              │
              │  Step 2: AES-256-CFB Encrypt
              ▼
┌────────────────────────────────┐
│  IV (16) | Ciphertext          │  Encrypted payload
└────────────────────────────────┘
              │
              │  Step 3: Calculate HMAC
              ▼
┌────────────────────────────────┐
│  HMAC-SHA256 (IV + Ciphertext) │  Authentication tag
└────────────────────────────────┘
              │
              │  Step 4: Prepend HMAC
              ▼
┌────────────────────────────────┐
│  HMAC (32) | IV (16) | Cipher  │  Final packet
└────────────────────────────────┘
              │
              │  Step 5: Send via UDP
              ▼
         [Network]

DECRYPTION (Reverse Process)
┌────────────────────────────────┐
│  HMAC (32) | IV (16) | Cipher  │  Received packet
└────────────────────────────────┘
              │
              │  Step 1: Split packet
              ▼
┌───────────┐ ┌────────────────┐
│ HMAC (32) │ │ IV (16)|Cipher │
└───────────┘ └────────────────┘
      │              │
      │              │  Step 2: Verify HMAC
      └──────┬───────┘
             │
             ▼
      ┌────────────┐
      │   Valid?   │───No──► Drop packet
      └────┬───────┘
           │ Yes
           ▼
      Extract IV and Ciphertext
           │
           │  Step 3: AES-256-CFB Decrypt
           ▼
┌────────────────────────────────┐
│  IP Header | TCP/UDP | Data    │  Original packet restored
└────────────────────────────────┘
```

---

## 🗺️ Network Topology

```
                    INTERNET
                        │
        ────────────────┼────────────────
        │                               │
        │                               │
    [Your ISP]                  [Italy Server ISP]
        │                               │
        │                               │
    Your Router                   Server Firewall
    (Your Home)                   (UDP 1194 open)
        │                               │
        │                               │
    Your Laptop ◄──────Encrypted───────► VPN Server
   (Client)            Tunnel           (Italy)
        │                               │
   10.8.0.2/24                      10.8.0.1/24
        │                               │
        └────────Virtual Network────────┘
              (10.8.0.0/24 subnet)

Traffic Flow:
1. App → Client TUN (10.8.0.2)
2. Encrypt → Send to Server
3. Server decrypts → TUN (10.8.0.1)
4. NAT to Internet (appears from Italy IP)
5. Response → Encrypt → Send to Client
6. Client decrypts → App receives
```

---

## 🔄 Routing Tables

### Client Routing (After Connection)

```
Destination     Gateway         Genmask         Interface
─────────────────────────────────────────────────────────
0.0.0.0         0.0.0.0         128.0.0.0       tun0     ◄ Default route
128.0.0.0       0.0.0.0         128.0.0.0       tun0     ◄ Default route
10.8.0.0        0.0.0.0         255.255.255.0   tun0     ◄ VPN subnet
SERVER_IP       Original GW     255.255.255.255 eth0     ◄ VPN server route
192.168.1.0     192.168.1.1     255.255.255.0   eth0     ◄ Local network

Result: All traffic goes through tun0 (VPN) except:
- Traffic to VPN server (goes direct to avoid loop)
- Local network traffic
```

### Server Routing

```
Destination     Gateway         Genmask         Interface
─────────────────────────────────────────────────────────
0.0.0.0         ISP_GATEWAY     0.0.0.0         eth0     ◄ Default route
10.8.0.0        0.0.0.0         255.255.255.0   tun0     ◄ VPN subnet

NAT Rule (iptables):
POSTROUTING -s 10.8.0.0/24 -o eth0 -j MASQUERADE

Result: Packets from VPN clients (10.8.0.0/24) get NATted
        to server's public IP before going to internet
```

---

## 📊 Component Interaction

```
┌────────────────────────────────────────────────────────┐
│                    CLIENT PROCESS                      │
├────────────────────────────────────────────────────────┤
│                                                        │
│  ┌──────────────┐         ┌──────────────┐           │
│  │   Goroutine  │         │   Goroutine  │           │
│  │   Incoming   │         │   Outgoing   │           │
│  │  (UDP→TUN)   │         │  (TUN→UDP)   │           │
│  └──────┬───────┘         └──────▲───────┘           │
│         │                        │                    │
│         │ Decrypt                │ Encrypt            │
│         ▼                        │                    │
│  ┌──────────────┐         ┌─────┴────────┐           │
│  │ UDP Socket   │◄───────►│ TUN Device   │           │
│  │ (to server)  │         │ (10.8.0.2)   │           │
│  └──────────────┘         └──────────────┘           │
│         ▲                        │                    │
│         │                        ▼                    │
│         │                  OS Network Stack           │
│         │                        │                    │
│         │                        ▼                    │
│         │                  Applications               │
└─────────┼────────────────────────┼────────────────────┘
          │                        │
          │  Encrypted Tunnel      │
          └────────────────────────┘


┌────────────────────────────────────────────────────────┐
│                    SERVER PROCESS                      │
├────────────────────────────────────────────────────────┤
│                                                        │
│  ┌──────────────┐         ┌──────────────┐           │
│  │   Goroutine  │         │   Goroutine  │           │
│  │   Incoming   │         │   Outgoing   │           │
│  │  (UDP→TUN)   │         │  (TUN→UDP)   │           │
│  └──────┬───────┘         └──────▲───────┘           │
│         │                        │                    │
│         │ Decrypt                │ Encrypt            │
│         ▼                        │                    │
│  ┌──────────────┐         ┌─────┴────────┐           │
│  │ UDP Socket   │◄───────►│ TUN Device   │           │
│  │ (:1194)      │         │ (10.8.0.1)   │           │
│  └──────────────┘         └──────────────┘           │
│         ▲                        │                    │
│         │                        ▼                    │
│         │                  OS Network Stack           │
│         │                        │                    │
│         │                        ▼                    │
│         │                  iptables NAT               │
│         │                        │                    │
│         │                        ▼                    │
│         │                     Internet                │
└─────────┼────────────────────────┼────────────────────┘
          │                        │
          │  Encrypted Tunnel      │
          └────────────────────────┘
```

---

## 🎯 Summary

- **Two goroutines** per process (incoming, outgoing)
- **AES-256-CFB** encryption with **HMAC-SHA256** authentication
- **TUN interfaces** for packet injection
- **UDP** for fast, lightweight transport
- **NAT** on server for internet access
- **Bidirectional** traffic support

All diagrams show the complete, production-ready VPN system! 🛡️
