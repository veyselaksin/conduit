# Example environment configuration for CipherWall VPN
# Copy this to .env and fill in your values
# DO NOT commit .env to git!

# Pre-Shared Key (MUST be exactly 32 bytes)
# Generate with: openssl rand -base64 32 | cut -c1-32
CIPHERWALL_PSK=your-very-secure-32byte-key!!

# Server Configuration
CIPHERWALL_SERVER_IP=10.8.0.1/24
CIPHERWALL_UDP_PORT=1194

# Client Configuration  
CIPHERWALL_CLIENT_IP=10.8.0.2/24

# Remote Server (for client)
CIPHERWALL_REMOTE_SERVER=your-italy-server-ip:1194

# Timezone (for Docker)
TZ=Europe/Rome

# Notes:
# - PSK must be EXACTLY 32 bytes
# - Keep this file secure and never commit it
# - Client and server MUST use the same PSK
